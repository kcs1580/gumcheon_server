<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="rptPjt.com.mapper.UserMapper">

	<select id="selectUserCount" resultType="int">
	select count(*) from user</select>
	
	
	<select id="selectUser" resultType="rptPjt.com.model.UserVO">
	SELECT id, password, name FROM user where id=#{id}</select>
	
	<insert id="insertUser">
	insert into user (id, password, name) values (#{id}, #{password}, #{name})</insert>
	
	<update id="updateUser">
	update user set password=#{password}, name=#{name} where id=#{id}</update>
	
	<delete id="deleteUser"> delete from user where id=#{id}</delete>
	
	<select id="selectUsers" resultType="rptPjt.com.model.UserVO">
	SELECT id, password, name FROM user
	</select>
	
	
</mapper>